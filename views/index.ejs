<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="../stylesheets/style.css" />
    <title>Files</title>
  </head>
  <body>
    <div class="main w-full min-h-screen bg-zinc-900 text-white relative">
      <h1 class="selection-none text-[10vw] absolute top-1/2 left-1/2 -translate-y-1/2 -translate-x-1/2 text-zinc-800 z-[0]">Tasks</h1>
      <div class="form relative p-10 z-[5]">
        <form action="/create" method="post">
          <input
            class="w-full px-4 py-3 bg-zinc-800 outline-none rounded-md"
            type="text"
            name="filename"
            placeholder="Title goes here..."
            required
          />
          <textarea
            class="w-full px-4 py-3 resize-none bg-zinc-800 outline-none rounded-md mt-3"
            type="text"
            name="details"
            placeholder="Write your task details here..."
            rows="4"
            required
          ></textarea>
          <input
            class="py-1 px-3 bg-blue-700 rounded-md mt-4"
            type="submit"
            value="Create Task"
          />
        </form>
      

      <div class="cards relative flex w-full flex-wrap gap-5 mt-8 z-[999]">
        <% if(files.length > 0) { %> <% files.forEach((val) => { %>
        <div class="card rounded-lg bg-zinc-700 py-2 px-3">
          <div class="flex justify-between gap-4 items-center">
            <h2 class="font-medium text-[20px]"><%= val %></h2>
            <a href="/delete/<%= val %>" class="text-red-400 text-sm">delete</a>
          </div>
          <div class="flex justify-between items-center gap-8">
            <a href="/file/<%= val %>" class="text-blue-400 text-sm">Read more...</a>
            <a href="/edit/<%= val %>" class="text-zinc-400 text-sm">edit filename</a>
          </div>
        </div>
        <% }) %> <%} else{%>
        <h3 class="text-zinc-600 font-medium">no tasks yet.</h3>
        <% } %>
      </div>
    </div>
  </div>
  </body>
</html>
